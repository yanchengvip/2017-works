<p class="sub-header">卡牌商城>卡牌促销>至尊宝箱列表</p>

<div class="lead text-right well">
  <p><%= link_to '修改', edit_extreme_chest_path(@extreme_chest), class: 'btn btn-primary', role: 'button' %></p>
</div>

<div class="panel panel-info">
  <!-- Default panel contents -->
  <div class="panel-heading"><strong>基础信息</strong></div>
  <ul class="list-group">

    <li class="list-group-item">前台启用：<%= status_str(@extreme_chest.status) %></li>
    <li class="list-group-item">时间段内累计使用能量满
      <span class="red"><%= @extreme_chest.card_count %></span>点时，赠
    </li>
    <li class="list-group-item">累计使用能量点数清零（重置）时间：每天
      <span class="red"><%= @extreme_chest.refresh_time %></span>
    </li>
  </ul>

  <div class="panel-heading"><strong>赠品内容</strong></div>

  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>赠品类型</th>
        <th width="30%">赠品范围</th>
        <th>赠品数量</th>
      </tr>
    </thead>
    <tbody>
      <% @extreme_chest.extreme_chest_items.each do |extreme_chest_item| %>
        <tr class="fields content_tr">
          <td><%= extreme_chest_item.gift_type == 1 ? '计谋' : '能量' %></td>
          <td><%= extreme_chest_item_gift_range_str(extreme_chest_item) %></td>
          <td><%= extreme_chest_item.gift_count %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

</div>
