<%= form_for(mammon_invite_record) do |f| %>
  <% if mammon_invite_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mammon_invite_record.errors.count, "error") %> prohibited this mammon_invite_record from being saved:</h2>

      <ul>
      <% mammon_invite_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
