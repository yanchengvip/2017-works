<%= form_for(mammon_user_card) do |f| %>
  <% if mammon_user_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mammon_user_card.errors.count, "error") %> prohibited this mammon_user_card from being saved:</h2>

      <ul>
      <% mammon_user_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
