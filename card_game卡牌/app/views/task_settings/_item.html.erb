<div class="form-group row">
  <div class=" col-sm-10">
    <hr><h3>任务完成奖励</h3>
    <table class="table table-striped table-hover mt_20" id="task_items_table">
      <thead>
        <tr class="info">
          <th>赠品类型</th>
          <th width="30%">赠品范围<span class="gift_range_text"></span></th>
          <th>赠品数量</th>
          <th></th>
        </tr>
      </thead>
      <tbody id="task_items_body">
        <% if @task_setting.task_items.present? %>
          <%= render partial: 'item_own' %>
        <% end %>
      </tbody>
    </table>
    <p data-container="body" data-toggle="popover" data-trigger="focus" data-placement="bottom" data-content="没有添加赠品内容" id="task_items_tip"></p>

    <%= link_to '添加', 'javascript:void(0)', id: 'add_task_item', class: 'btn btn-success' %>
  </div>
</div>

<%= render partial: 'item_type_modal' %>
<%= render partial: '/packages/error_modal' %>


<script type="text/javascript">
  // $(document).on('turbolinks:load', function () {
  $(function(){
    // 添加
    $('#add_task_item').on('click', function(){
      // if($('.content_tr').length >= 1){
      //   elert('能量只能添加一个！')
      //   return false
      // }
      $('#choseTaskItemTypeModal').modal('show')
    })
  })
</script>
