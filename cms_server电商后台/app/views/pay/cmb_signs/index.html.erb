<p id="notice"><%= notice %></p>

<h1>Pay Cmb Signs</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pay_cmb_signs.each do |pay_cmb_sign| %>
      <tr>
        <td><%= link_to 'Show', pay_cmb_sign %></td>
        <td><%= link_to 'Edit', edit_pay_cmb_sign_path(pay_cmb_sign) %></td>
        <td><%= link_to 'Destroy', pay_cmb_sign, method: :delete, data: { confirm: '
Are you sure?

你确定吗？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pay Cmb Sign', new_pay_cmb_sign_path %>
