<p id="notice"><%= notice %></p>

<h1>Auction Provinces</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @auction_provinces.each do |auction_province| %>
      <tr>
        <td><%= link_to 'Show', auction_province %></td>
        <td><%= link_to 'Edit', edit_auction_province_path(auction_province) %></td>
        <td><%= link_to 'Destroy', auction_province, method: :delete, data: { confirm: '
Are you sure?

你确定吗？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Auction Province', new_auction_province_path %>
