<%= form_with(model: auction_fanship, local: true) do |form| %>
  <% if auction_fanship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_fanship.errors.count, "error") %> prohibited this auction_fanship from being saved:</h2>

      <ul>
      <% auction_fanship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
