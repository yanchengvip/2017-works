<%= form_for @auction_attribute, url: auction_attribute_path,:method=>"put",  html: {class: 'bg pd_20'} do |f| %>
  <%= f.hidden_field :id %>
  <%= render partial: 'form', locals: { f: f} %>
<% end %>
