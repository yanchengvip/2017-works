<!-- <p id="notice"><%= notice %></p>

<h1>Auction Levels</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @auction_levels.each do |auction_level| %>
      <tr>
        <td><%= link_to 'Show', auction_level %></td>
        <td><%= link_to 'Edit', edit_auction_level_path(auction_level) %></td>
        <td><%= link_to 'Destroy', auction_level, method: :delete, data: { confirm: '
Are you sure?

你确定吗？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Auction Level', new_auction_level_path %> -->
