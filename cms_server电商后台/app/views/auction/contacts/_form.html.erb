<%= form_with(model: auction_contact, local: true) do |form| %>
  <% if auction_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_contact.errors.count, "error") %> prohibited this auction_contact from being saved:</h2>

      <ul>
      <% auction_contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
