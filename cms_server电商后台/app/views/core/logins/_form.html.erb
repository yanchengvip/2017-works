<%= form_with(model: core_login, local: true) do |form| %>
  <% if core_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(core_login.errors.count, "error") %> prohibited this core_login from being saved:</h2>

      <ul>
      <% core_login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
