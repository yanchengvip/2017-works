<p id="notice"><%= notice %></p>

<h1>Core Connections</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @core_connections.each do |core_connection| %>
      <tr>
        <td><%= link_to 'Show', core_connection %></td>
        <td><%= link_to 'Edit', edit_core_connection_path(core_connection) %></td>
        <td><%= link_to 'Destroy', core_connection, method: :delete, data: { confirm: '
Are you sure?

你确定吗？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Core Connection', new_core_connection_path %>
