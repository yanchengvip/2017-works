<%= form_with(model: retail_cart, local: true) do |form| %>
  <% if retail_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(retail_cart.errors.count, "error") %> prohibited this retail_cart from being saved:</h2>

      <ul>
      <% retail_cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
