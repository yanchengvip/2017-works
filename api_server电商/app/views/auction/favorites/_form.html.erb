<%= form_with(model: auction_favorite, local: true) do |form| %>
  <% if auction_favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_favorite.errors.count, "error") %> prohibited this auction_favorite from being saved:</h2>

      <ul>
      <% auction_favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
