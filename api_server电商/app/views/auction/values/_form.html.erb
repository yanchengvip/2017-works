<%= form_with(model: auction_value, local: true) do |form| %>
  <% if auction_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_value.errors.count, "error") %> prohibited this auction_value from being saved:</h2>

      <ul>
      <% auction_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
