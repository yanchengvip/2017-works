<%= form_with(model: auction_coupon, local: true) do |form| %>
  <% if auction_coupon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_coupon.errors.count, "error") %> prohibited this auction_coupon from being saved:</h2>

      <ul>
      <% auction_coupon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
