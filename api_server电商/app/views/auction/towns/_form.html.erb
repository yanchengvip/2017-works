<%= form_with(model: auction_town, local: true) do |form| %>
  <% if auction_town.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_town.errors.count, "error") %> prohibited this auction_town from being saved:</h2>

      <ul>
      <% auction_town.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
