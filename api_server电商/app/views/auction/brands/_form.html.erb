<%= form_with(model: auction_brand, local: true) do |form| %>
  <% if auction_brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_brand.errors.count, "error") %> prohibited this auction_brand from being saved:</h2>

      <ul>
      <% auction_brand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
