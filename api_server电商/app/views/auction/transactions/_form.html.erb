<%= form_with(model: auction_transaction, local: true) do |form| %>
  <% if auction_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_transaction.errors.count, "error") %> prohibited this auction_transaction from being saved:</h2>

      <ul>
      <% auction_transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
