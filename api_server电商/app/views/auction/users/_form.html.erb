<%= form_with(model: auction_user, local: true) do |form| %>
  <% if auction_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_user.errors.count, "error") %> prohibited this auction_user from being saved:</h2>

      <ul>
      <% auction_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
