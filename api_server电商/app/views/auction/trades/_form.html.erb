<%= form_with(model: auction_trade, local: true) do |form| %>
  <% if auction_trade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_trade.errors.count, "error") %> prohibited this auction_trade from being saved:</h2>

      <ul>
      <% auction_trade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
