<%= form_with(model: auction_card, local: true) do |form| %>
  <% if auction_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_card.errors.count, "error") %> prohibited this auction_card from being saved:</h2>

      <ul>
      <% auction_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
