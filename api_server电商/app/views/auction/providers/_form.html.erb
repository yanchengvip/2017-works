<%= form_with(model: auction_provider, local: true) do |form| %>
  <% if auction_provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(auction_provider.errors.count, "error") %> prohibited this auction_provider from being saved:</h2>

      <ul>
      <% auction_provider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
