<%= form_with(model: manage_grant, local: true) do |form| %>
  <% if manage_grant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manage_grant.errors.count, "error") %> prohibited this manage_grant from being saved:</h2>

      <ul>
      <% manage_grant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
