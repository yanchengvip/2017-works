<%= form_with(model: dealer, local: true) do |form| %>
  <% if dealer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dealer.errors.count, "error") %> prohibited this dealer from being saved:</h2>

      <ul>
      <% dealer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dealer_name %>
  </div>

  <div class="field">
    <%= form.label :dealer_type %>
    <%= form.number_field :dealer_type, id: :dealer_dealer_type %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.number_field :status, id: :dealer_status %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :dealer_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
