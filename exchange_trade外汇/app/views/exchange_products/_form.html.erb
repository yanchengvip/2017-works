<%= form_with(model: exchange_product, local: true) do |form| %>
  <% if exchange_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exchange_product.errors.count, "error") %> prohibited this exchange_product from being saved:</h2>

      <ul>
      <% exchange_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :symbol %>
    <%= form.text_field :symbol, id: :exchange_product_symbol %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :exchange_product_active %>
  </div>

  <div class="field">
    <%= form.label :published %>
    <%= form.check_box :published, id: :exchange_product_published %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
